spring:
  application:
    name: precious-metals-tracker

  jpa:
    open-in-view: false

  flyway:
    enabled: true

logging:
  level:
    com.example.preciousmetals: INFO

app:
  cors:
    allowed-origins: http://localhost:5173

